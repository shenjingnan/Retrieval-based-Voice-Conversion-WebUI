# worktree 目录
.worktrees/

# 构建产物与本地运行时产物
__pycache__/
*.pyc
.pytest_cache/
server/static/
TEMP/

# 下载型模型资产、训练产物与用户上传的训练数据集（README 指引下载/训练生成，不入库）
# logs 不带斜杠：worktree 里的 logs 是指回主仓库的软链，带斜杠的规则只匹配目录、
# 会把这个软链漏成 untracked
assets/
logs
datasets/
.model-downloads/
.DS_Store
